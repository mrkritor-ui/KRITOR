(function(){const grid=document.getElementById("grid"),glassIntro=document.getElementById("glass-intro"),glassLogo=document.querySelector(".glass-logo"),zoomControl=document.getElementById("zoom-control"),shuffleControl=document.getElementById("shuffle-control");const levels=["zoom","normal","overview"],isTouchDevice=window.matchMedia("(hover:none) and (pointer:coarse)").matches;let level=isTouchDevice?0:2,shuffled=false,shuffleCount=0,distortion=0,shuffleLocked=false,pinchStart=null,gestureStartScale=null,gestureHandled=false;function applyLevel(n){level=(n+levels.length)%levels.length;grid.classList.remove("view-normal","view-zoom","view-overview");grid.classList.add(`view-${levels[level]}`);if(zoomControl){zoomControl.textContent=level===2?"−":"+";zoomControl.setAttribute("aria-label",level===2?"Return to larger catalogue view":"Reduce artwork size")}}function workNumber(w){const m=String(w.id||"").match(/work-(\d+)/i);return m?Number(m[1]):-1}function makeTile(w){const a=document.createElement("a"),i=document.createElement("img");a.className="tile";a.dataset.workId=w.id;a.href=`work.html?id=${encodeURIComponent(w.id)}`;a.setAttribute("aria-label",w.title?`${w.title}, ${w.year}`:`Artwork ${w.id}`);i.src=w.thumbnail||w.image;i.alt=w.title||"";i.loading="lazy";i.decoding="async";i.onerror=function(){if(w.thumbnail&&i.src.indexOf(w.thumbnail)!==-1)i.src=w.image};a.appendChild(i);return a}function orderedWorks(){const works=ARTWORKS.slice();return shuffled?works.sort(()=>Math.random()-.5):works.sort((a,b)=>workNumber(b)-workNumber(a))}function render(){grid.innerHTML="";if(!Array.isArray(ARTWORKS)||!ARTWORKS.length)return;orderedWorks().forEach(w=>grid.appendChild(makeTile(w)));applyLevel(level)}function unlock(ms){setTimeout(()=>{shuffleLocked=false;if(shuffleControl){shuffleControl.disabled=false;shuffleControl.removeAttribute("aria-disabled");shuffleControl.style.pointerEvents="";shuffleControl.style.opacity=""}},ms)}function normalShuffle(){const current=[...grid.children],next=orderedWorks(),byId=new Map(current.map(el=>[el.dataset.workId,el])),els=next.map(w=>byId.get(w.id)).filter(Boolean);if(els.length!==current.length){render();unlock(500);return}const positions=new Map(current.map((el,i)=>[el,i]));els.forEach((el,i)=>{const from=positions.get(el),to=i;if(from!==to){el.style.transition="transform 420ms cubic-bezier(.2,.8,.2,1)";el.style.transform=`translateY(${(from-to)*4}px)`}});requestAnimationFrame(()=>{els.forEach(el=>{grid.appendChild(el);el.style.transform=""});setTimeout(()=>{els.forEach(el=>el.style.transition="");unlock(520)},440)})}function patternShuffle(){const current=[...grid.children],next=orderedWorks(),byId=new Map(current.map(el=>[el.dataset.workId,el])),els=next.map(w=>byId.get(w.id)).filter(Boolean);if(els.length!==current.length){render();unlock(1000);return}const cols=Math.max(1,Math.round(grid.clientWidth/(parseFloat(getComputedStyle(grid).getPropertyValue("--tile-min"))||180))),rows=Math.ceil(els.length/cols),intensity=Math.min(4,distortion),pattern=new Set(),cx=Math.floor(cols/2),cy=Math.floor(rows*.45);for(let y=-2;y<=2+intensity;y++)for(let x=-2-intensity;x<=2+intensity;x++){const edge=Math.abs(x)+Math.abs(y);if(edge<=2+intensity&&Math.random()>.22){const c=cx+x,r=cy+y;if(c>=0&&c<cols&&r>=0&&r<rows)pattern.add(r*cols+c)}}const duration=700+intensity*180,maxShift=10+intensity*11;els.forEach((el,i)=>{el.style.transition=`transform ${duration}ms cubic-bezier(.16,1,.3,1),margin ${duration}ms cubic-bezier(.16,1,.3,1)`;if(pattern.has(i)){const x=(Math.random()-.5)*maxShift*2,y=(Math.random()-.5)*maxShift*2;el.style.transform=`translate(${x}px,${y}px) rotate(${(Math.random()-.5)*intensity*1.8}deg)`;el.style.marginLeft=`${(Math.random()-.5)*intensity*9}px`;el.style.marginTop=`${Math.random()*intensity*10}px`}});setTimeout(()=>{els.forEach(el=>el.style.transition="");unlock(duration+250)},duration+50)}function shuffle(){if(shuffleLocked)return;shuffleLocked=true;if(shuffleControl){shuffleControl.disabled=true;shuffleControl.setAttribute("aria-disabled","true");shuffleControl.style.pointerEvents="none";shuffleControl.style.opacity=".4"}shuffled=true;shuffleCount++;if(shuffleCount%4===0){distortion=Math.min(4,distortion+1);patternShuffle()}else if(distortion>0)patternShuffle();else normalShuffle();grid.scrollIntoView({block:"start",behavior:"smooth"})}render();if(zoomControl)zoomControl.addEventListener("click",()=>applyLevel(level+1));if(shuffleControl)shuffleControl.addEventListener("click",shuffle);function pd(t){const a=t[0],b=t[1];return Math.hypot(a.clientX-b.clientX,a.clientY-b.clientY)}grid.addEventListener("gesturestart",e=>{gestureStartScale=e.scale||1;gestureHandled=false;e.preventDefault()},{passive:false});grid.addEventListener("gesturechange",e=>{e.preventDefault();if(gestureHandled||gestureStartScale===null)return;const s=e.scale||1;if(Math.abs(s-gestureStartScale)>.12){applyLevel(level+(s>gestureStartScale?-1:1));gestureHandled=true}},{passive:false});grid.addEventListener("gestureend",e=>{e.preventDefault();gestureStartScale=null;gestureHandled=false},{passive:false});grid.addEventListener("touchstart",e=>{if(e.touches.length===2)pinchStart=pd(e.touches)},{passive:true});grid.addEventListener("touchmove",e=>{if(e.touches.length!==2||pinchStart===null)return;const d=pd(e.touches)-pinchStart;if(Math.abs(d)>80){applyLevel(level+(d>0?-1:1));pinchStart=pd(e.touches)}},{passive:true});grid.addEventListener("touchend",()=>pinchStart=null,{passive:true});const returnToCatalogue=new URLSearchParams(location.search).get("return");if(returnToCatalogue==="catalogue"&&glassIntro){glassIntro.classList.add("clear");glassIntro.setAttribute("aria-hidden","true");return}if(glassIntro){function dismiss(e){e.preventDefault();e.stopPropagation();glassIntro.classList.add("clear");glassIntro.setAttribute("aria-hidden","true")}glassIntro.addEventListener("click",dismiss,{capture:true});glassIntro.addEventListener("touchend",dismiss,{capture:true,passive:false});if(glassLogo)glassLogo.addEventListener("click",dismiss,{capture:true})}})();