(function(){const p=new URLSearchParams(location.search),id=p.get('id'),page=document.getElementById('work-page'),work=ARTWORKS.find(w=>w.id===id);if(!work){page.innerHTML='<div class="work"><p>Work not found.</p></div>';return}const related=ARTWORKS.filter(w=>w.id!==work.id&&w.collection&&w.collection===work.collection),others=ARTWORKS.filter(w=>w.id!==work.id&&!related.includes(w)),similar=[...related,...others].slice(0,8),price=work.status==='sold'?'SOLD':work.status==='available'?(work.price?'$'+Number(work.price).toLocaleString()+' · AVAILABLE':'AVAILABLE'):'NOT CURRENTLY AVAILABLE',text=work.text||work.description||'';page.innerHTML=`<div class="work"><div class="art-wrap" id="art-wrap"><img class="art" src="${work.image}" alt="${work.title||'Artwork'}"></div><section class="meta"><div class="title">${work.title||'Untitled'}</div><div class="year">${work.year||''}</div><div class="details"><span class="label">Series</span><span>${work.collection||'—'}</span><span class="label">Materials</span><span>${work.materials||'—'}</span><span class="label">Size</span><span>${work.size||'—'}</span><span class="label">Status</span><span>${price}</span><span class="label">Collection</span><span>${work.owner||'—'}</span><span class="label">Viewing</span><span>${work.viewing||'Not currently on view'}</span></div>${text?'<button class="text-toggle" id="text-toggle">Text — click to reveal</button><div class="text" id="text">'+text+'</div>':''}${work.ar&&work.ar.enabled?'<a class="ar" href="'+work.ar.file+'">View in AR</a>':''}</section><section class="similar"><h2>Similar works</h2><div class="similar-track">${similar.map(w=>`<a href="work.html?id=${encodeURIComponent(w.id)}"><img src="${w.thumbnail||w.image}" alt="${w.title||'Artwork'}" loading="lazy" onerror="this.onerror=null;this.src='${w.image}'"></a>`).join('')}</div></section></div><div class="inspect" id="inspect"><img id="inspect-img" src="${work.image}" alt=""></div>`;const t=document.getElementById('text-toggle');if(t)t.onclick=()=>document.getElementById('text').classList.toggle('open');const wrap=document.getElementById('art-wrap'),ins=document.getElementById('inspect'),img=document.getElementById('inspect-img');let dist=0,scale=1,start=1,tx=0,ty=0,lastX=0,lastY=0;function render(){img.style.transform=`translate3d(${tx}px,${ty}px,0) scale(${scale})`}function open(){ins.classList.add('active');document.body.style.overflow='hidden';scale=1;tx=ty=0;render()}function close(){ins.classList.remove('active');document.body.style.overflow='';setTimeout(()=>{scale=1;tx=ty=0;render()},650)}wrap.addEventListener('touchstart',e=>{if(e.touches.length===2){e.preventDefault();open();dist=Math.hypot(e.touches[0].clientX-e.touches[1].clientX,e.touches[0].clientY-e.touches[1].clientY);start=1}},{passive:false});wrap.addEventListener('touchmove',e=>{if(e.touches.length===2){e.preventDefault();const d=Math.hypot(e.touches[0].clientX-e.touches[1].clientX,e.touches[0].clientY-e.touches[1].clientY);scale=Math.max(1,Math.min(5,start*d/dist));render()}},{passive:false});wrap.addEventListener('touchend',e=>{if(e.touches.length===0&&ins.classList.contains('active'))setTimeout(close,180)},{passive:true});ins.addEventListener('touchstart',e=>{e.preventDefault();if(e.touches.length===2){dist=Math.hypot(e.touches[0].clientX-e.touches[1].clientX,e.touches[0].clientY-e.touches[1].clientY);start=scale}else if(e.touches.length===1){lastX=e.touches[0].clientX;lastY=e.touches[0].clientY}},{passive:false});ins.addEventListener('touchmove',e=>{e.preventDefault();if(e.touches.length===2){const d=Math.hypot(e.touches[0].clientX-e.touches[1].clientX,e.touches[0].clientY-e.touches[1].clientY);scale=Math.max(1,Math.min(5,start*d/dist));render()}else if(e.touches.length===1&&scale>1){tx+=e.touches[0].clientX-lastX;ty+=e.touches[0].clientY-lastY;lastX=e.touches[0].clientX;lastY=e.touches[0].clientY;render()}},{passive:false});ins.addEventListener('touchend',e=>{if(e.touches.length===0)setTimeout(close,180)},{passive:true});document.addEventListener('gesturestart',e=>e.preventDefault(),{passive:false});document.addEventListener('gesturechange',e=>e.preventDefault(),{passive:false});document.addEventListener('gestureend',e=>e.preventDefault(),{passive:false});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()})})();